/**
 * Update stats display
 * @param {Object} stats
 */
export function updateStats(stats) {
  // Stats are updated directly in app.js via sidebar stat elements
  // This module exists for future expansion (e.g., top metrics row)
}
